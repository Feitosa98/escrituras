# Variáveis de ambiente

# Ambiente (development ou production)
NODE_ENV=development

# Porta do servidor
PORT=3001

# Chave secreta para JWT (ALTERAR EM PRODUÇÃO!)
JWT_SECRET=sua-chave-secreta-super-segura-aqui-2026-feitosa-solucoes

# Caminho do banco de dados (opcional, usa padrão se não definido)
# DB_PATH=./database/escrituras.db
